"""
📋 المهام المتقدمة
مهام الفريق الذكي
"""

from crewai import Task
from agents import (
    super_researcher, 
    technical_analyst, 
    project_manager
)

def create_research_task(query: str) -> Task:
    """
    إنشاء مهمة بحث متقدمة
    """
    return Task(
        description=f"""
        قم بالبحث المتقدم عن: {query}
        
        المطلوب:
        1. ابحث في الويب عن أفضل المعلومات
        2. ابحث عن فيديوهات YouTube ذات الصلة
        3. استخرج محتوى الصفحات المهمة
        4. جمع جميع الروابط والمصادر الموثوقة
        
        تأكد من جودة المعلومات والروابط.
        """,
        agent=super_researcher,
        expected_output="""
        قائمة شاملة تتضمن:
        - روابط موثوقة من الويب
        - فيديوهات YouTube ذات الصلة
        - ملخصات المحتوى المهم
        - تقييم موثوقية المصادر
        """,
        verbose=True
    )

def create_analysis_task(research_results: str) -> Task:
    """
    إنشاء مهمة تحليل متقدمة
    """
    return Task(
        description=f"""
        قم بتحليل نتائج البحث التالية:
        {research_results}
        
        المطلوب:
        1. تحليل المعلومات بعمق
        2. استخراج الكلمات المفتاحية
        3. صياغة إجابة احترافية بالعربية
        4. تنظيم البيانات بطريقة واضحة
        
        تأكد من الدقة والاحترافية.
        """,
        agent=technical_analyst,
        expected_output="""
        إجابة احترافية تتضمن:
        - ملخص شامل
        - نقاط مهمة
        - الكلمات المفتاحية
        - روابط مفيدة
        - توصيات
        """,
        verbose=True
    )

def create_coordination_task(research: str, analysis: str) -> Task:
    """
    إنشاء مهمة تنسيق
    """
    return Task(
        description=f"""
        قم بتنسيق النتائج النهائية:
        
        البحث: {research}
        التحليل: {analysis}
        
        المطلوب:
        1. دمج النتائج بشكل متناسق
        2. التأكد من الجودة
        3. إضافة لمسات احترافية
        4. إعداد التقرير النهائي
        """,
        agent=project_manager,
        expected_output="""
        تقرير نهائي احترافي يتضمن:
        - ملخص تنفيذي
        - النتائج المفصلة
        - الروابط والمراجع
        - التوصيات
        - الخلاصة
        """,
        verbose=True
    )

def get_research_task(query: str):
    """الحصول على مهمة البحث"""
    return create_research_task(query)

def get_analysis_task(research_results: str):
    """الحصول على مهمة التحليل"""
    return create_analysis_task(research_results)

def get_coordination_task(research: str, analysis: str):
    """الحصول على مهمة التنسيق"""
    return create_coordination_task(research, analysis)
